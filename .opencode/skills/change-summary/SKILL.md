---
name: change-summary
description: 生成本地变更说明。Use when 变更/summary/提交流程；借鉴 PR-Agent /describe，不提交。
---

# change-summary — 变更说明

生成本地改动的变更说明（不提交）：
- 背景 / 改动摘要 / 影响面 / 测试结果 / 自审结论

借鉴 PR-Agent `/describe` 的结构。

> 格式规范写进 `coder` agent 文件（待后定，先空着）。
