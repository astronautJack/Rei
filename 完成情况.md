# Rei 完成情况

> 开发与验证状态总览。**每次开发或验证后同步更新本文件**（不进 `.gitignore`，纳入版本管理）。
> 最近更新：2026-07-23

## 成品接线

| 项 | 状态 | 说明 |
|---|---|---|
| CRG（code-review-graph v2.3.7） | ✅ 已装+已验 | `~/.local/bin/code-review-graph`，`--repo` 任意路径；avsession 实跑建图 568 文件/8282 节点/74273 边 |
| DCP（opencode-dcp） | ✅ 已装 / ⚠️ 阈值待验 | 全局插件；`~/.config/opencode/dcp.jsonc` 已改百分比阈值 max`75%`/min`45%`（需重启 opencode 生效，未在长任务中验证兜底） |

## .opencode/ 配置

| 项 | 状态 | 说明 |
|---|---|---|
| 8 agents（rei/cartographer/wiki-writer/bug-tracer/planner/coder/reviewer/tester） | ✅ 已写 | 见 `.opencode/agents/` |
| 7 commands（/wiki-map /wiki-doc /feature /bug /review /gates /stage） | ✅ 已写 | 命令名已重命名（`/wiki`→`/wiki-map`、`/wiki-prose`→`/wiki-doc`） |
| `opencode.json` + `AGENTS.md` | ✅ 已写 | 权限 / compaction / default_agent=rei |

## 三条工作流

| 流 | 命令 | 开发 | 验证 | 说明 |
|---|---|---|---|---|
| WF1 代码→Wiki·结构页 | `/wiki-map` | ✅ | ✅ | avsession_wiki/ 14 社区页（gitignored，可再生） |
| WF1 代码→Wiki·架构文档 | `/wiki-doc` | ✅ | ✅ | avsession_wiki_prose/ 14 社区全量生成（gitignored）；wiki-writer 已支持按社区增量刷新（last_sync_commit + git diff + 文件→社区映射） |
| WF2 需求端到端 | `/feature` | ✅ | ⏳ 未验 | 流程已写，缺端到端 demo |
| WF3 BUG 定位 | `/bug` | ✅ | ⏳ 未验 | 流程已写，缺定位 demo |

## 文档

| 文件 | 状态 | 说明 |
|---|---|---|
| `方案设计.md` | ✅ 同步 | 设计稿，含 §11 备选方案调研 |
| `README.md` | ✅ 同步 | 用户使用说明 |
| `AGENTS.md` | ✅ 同步 | 产品 agent-facing（工具集使用约定，非开发约定） |
| `完成情况.md` | ✅ 新建 | 本文件，每次开发/验证后更新 |

## 环境备注

- 生产：Windows + Git Bash（opencode shell 指向 Git Bash）；当前开发：Linux（WSL）。命令一致。
- 备份：`/home/dlrow_hl/AgentRei`（手工备份，非开发目录）。
- 开发工作区：`/home/dlrow_hl/ReiDev`（独立 opencode 工作区，`AGENTS.md` 指导开发，指向本仓）。

## 待办

- [ ] WF2 `/feature` 端到端 demo（用 avsession 仓跑一遍设计→实现→审→测→更新 wiki）
- [ ] WF3 `/bug` 定位 demo
- [ ] DCP 阈值（75%/45%）在长任务中的兜底验证
- [ ] Windows + Git Bash 生产实跑验证
